# ── Dependencies ──
node_modules/

# ── Build artifacts ──
dist/
out/
build/
*.dmg
*.exe
*.AppImage
*.deb
*.rpm
*.snap
*.blockmap

# ── Platform FFmpeg binaries ──
# Fetched by `npm run ffmpeg:fetch` (Windows/Linux) or built by
# scripts/build-ffmpeg-macos.sh. Never committed — they are large, and the
# pinned URL + checksum + licence gate is the record of what they are.
src/bin/ffmpeg
src/bin/ffprobe
src/bin/ffmpeg.exe
src/bin/ffprobe.exe
src/bin/ffmpeg-darwin-*
src/bin/ffprobe-darwin-*
src/bin/FFMPEG-LICENSE.txt
.ffmpeg-cache/
.ffmpeg-build/

# ── Signing & certificates ──
*.p12
*.p8
*.cer
*.pem
*.key
*.provisionprofile

# ── macOS artifacts ──
.DS_Store
*.app/

# ── Editor / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Replit ──
.cache/
.config/
.upm/
.local/
replit_zip_error_log.txt

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── Environment ──
.env
.env.local
.env.production

# Local test media — large binaries, not part of the app
FOOTAGE/
