<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for Electron's V8 JavaScript engine JIT compilation -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Required for Electron's V8 engine to allocate executable memory -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Required for loading bundled ffmpeg binaries (unsigned native executables) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Allow reading files the user opens via Open dialog, drag-and-drop, or Finder -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Allow reading files from any location (for command-line file arguments) -->
    <key>com.apple.security.files.bookmarks.app-scope</key>
    <true/>
</dict>
</plist>
